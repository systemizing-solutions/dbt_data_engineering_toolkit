# Generated by det 6.0.0. Edit the workbook, not this file.
version: 2
models:
- name: stg_customers
  description: Cleaned and validated customer records
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: urn:datacontract:customer:customer_360
        owner: Customer Data
  columns:
  - name: customer_id
    description: Stable customer identifier
    data_type: varchar
    data_tests:
    - not_null:
        config:
          meta:
            datacontract_cli:
              check: stg_customers__customer_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field customer_id has no missing values
    - unique:
        config:
          meta:
            datacontract_cli:
              check: stg_customers__customer_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field customer_id has no duplicate values
  - name: customer_name
    description: Normalised customer name
    data_type: varchar
    data_tests:
    - not_null:
        config:
          meta:
            datacontract_cli:
              check: stg_customers__customer_name__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field customer_name has no missing values
  - name: email
    description: Lower-case validated email
    data_type: varchar
  - name: revenue
    description: Customer lifetime revenue
    data_type: decimal(18,2)
  - name: event_date_display
    description: Mapped and formatted event date
    data_type: varchar
  - name: status_label
    description: Readable account status
    data_type: varchar
    data_tests:
    - not_null:
        config:
          meta:
            datacontract_cli:
              check: stg_customers__status_label__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field status_label has no missing values
  - name: _det_rejections
    description: Reject-row rule IDs that failed for the record.
    data_type: varchar[]
    assertions:
      email_invalid:
        description: Reject malformed or missing emails
        expression: _email_invalid_valid
        null_as_exception: true
  - name: _det_warnings
    description: Warning rule IDs that failed for the record.
    data_type: varchar[]
    assertions:
      revenue_not_positive:
        description: Keep the row but publish a warning
        expression: _revenue_not_positive_valid
        null_as_exception: true
      customer_id_operational_warning:
        description: Operational visibility only; ODCS Required remains the contract authority
        expression: _customer_id_operational_warning_valid
        null_as_exception: true
  - name: _det_loaded_at
    description: Time the dbt model produced the record.
    data_type: timestamp with time zone
  - name: _det_invocation_id
    description: dbt invocation that produced the record.
    data_type: varchar
  - name: _det_source_system
    description: Declared source system for lineage.
    data_type: varchar
