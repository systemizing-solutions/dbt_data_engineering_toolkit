# Generated by det 5.0.0. Edit the workbook, not this file.
version: 2
models:
- name: stg_customers
  description: Clean customer identities
  config:
    contract:
      enforced: true
    meta:
      datacontract_cli:
        contract_id: urn:datacontract:customer:customer_accounts
        owner: customer
  columns:
  - name: customer_id
    description: Stable customer identifier.
    data_type: varchar
    data_tests:
    - not_null:
        config:
          meta:
            datacontract_cli:
              check: stg_customers__customer_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field customer_id has no missing values
    - unique:
        config:
          meta:
            datacontract_cli:
              check: stg_customers__customer_id__field_unique
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field customer_id has no duplicate values
  - name: account_id
    description: Account join key.
    data_type: varchar
    data_tests:
    - not_null:
        config:
          meta:
            datacontract_cli:
              check: stg_customers__account_id__field_required
              include_in_tests: true
              contract_versions:
              - 1.0.0
              generated: true
        description: Check that field account_id has no missing values
  - name: customer_name
    description: Clean display name.
    data_type: varchar
  - name: email
    description: Normalized contact email.
    data_type: varchar
  - name: status_label
    description: Business status mapped from a controlled lookup.
    data_type: varchar
  - name: _det_warnings
    description: Operational warning identifiers.
    data_type: varchar[]
    assertions:
      email_warning:
        description: Publish an operational warning for malformed email
        expression: _email_warning_valid
        null_as_exception: true
  - name: _det_loaded_at
    data_type: timestamp with time zone
  - name: _det_invocation_id
    data_type: varchar
  - name: _det_source_system
    data_type: varchar
