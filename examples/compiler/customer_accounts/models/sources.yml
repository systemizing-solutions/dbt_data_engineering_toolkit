# Generated by det 6.0.0. Edit the workbook, not this file.
version: 2
sources:
- name: raw
  schema: '{{ target.schema }}'
  tables:
  - name: raw_customers
    identifier: raw_customers
    description: Raw customer identity feed
    columns:
    - name: customer_id
      description: Stable source customer ID
      data_type: varchar
    - name: account_id
      description: Account join key
      data_type: varchar
    - name: customer_name
      description: Unclean customer name
      data_type: varchar
    - name: email
      description: Unclean customer email
      data_type: varchar
    - name: status_code
      description: Source status code
      data_type: varchar
  - name: raw_accounts
    identifier: raw_accounts
    description: Raw account attributes
    columns:
    - name: account_id
      description: Unique account key
      data_type: varchar
      data_tests:
      - unique
    - name: account_tier
      description: Account tier
      data_type: varchar
    - name: lifetime_value
      description: Lifetime value as received
      data_type: varchar
