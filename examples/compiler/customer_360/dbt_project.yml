# Generated by det 5.0.1. Edit the workbook, not this file.
name: customer_360
version: 1.0.0
config-version: 2
profile: customer_360
require-dbt-version:
- '>=1.10.6'
- <3.0.0
dispatch:
- macro_namespace: dbt
  search_order:
  - dbt_project_evaluator
  - dbt
model-paths:
- models
seed-paths:
- seeds
test-paths:
- tests
clean-targets:
- target
- dbt_packages
- logs
vars:
  exclude_packages:
  - all
models:
  customer_360:
    staging:
      +materialized: view
    quarantine:
      +materialized: view
